在线考试交卷答题剩余时间0小时00分00秒
1.
(单选题)使用命令unzip解压缩 文档时，选项（ b）可以指定解压缩的位置
 A.-r
 B.-d
 C.-O
 D.o
2.
(单选题)日志文件存放的路径是（ d）。
 A./etc/log
 B./tmp/log
 C./dev/log
 D./var/log
3.
(单选题)rsync的日志文件是(b )。
 A./etc/rsyncd.conf
 B./var/log/rsync.log
 C./var/log/rsyncd.log
 D./var/log/rsync/rsync.log
4.
(单选题)在RHEL7系统中yum仓库配置文件后缀名是（c ）。
 A..cfg
 B..Conf
 C..repo
 D..rpm
5.
(单选题)列出所有系统服务的命令是(d )。
 A.ls /etc/servers*
 B.systemctl -t server
 C.systemctl -t service
 D.systemctl -t service --all
6.
(单选题)在pxe一键装机中客户端由网卡pxe启动完毕后，主机引导权交给（b ）。
 A.syslinux
 B.pxelinux.0
 C.tftp
 D.http
7.
(单选题)自定义yum源时需要安装软件（ c）。
 A.无需安装软件
 B.无需安装软件，可直接拷贝软件包到可用yum源中
 C.需要安装createrepo
 D.需要安装yum.repo
8.
(单选题)创建卷组是如何指定PE大小( a)。
 A.vgcreate -s PE大小 卷组名 空闲分区
 B.lvcreate -s PE大小 卷组名 空闲分区
 C.vgextend -s PE大小 卷组名 空闲分区
 D.lvextend -s PE大小 卷组名 空闲分区
9.
(单选题)想把一个用户加入到组下的命令是什么 （d ）
 A.gpasswd -a 组名 用户名
 B.gpasswd -add 用户名 组名
 C.gpasswd -in 用户名 组名
 D.gpasswd -a 用户名 组名
10.
(单选题)使用mount命令挂载samba共享时需要安装软件包（ c）。
 A.不需要安装任何软件包，可直接挂载
 B.安装ntfs-3g
 C.安装cifs-utils
 D.不需要安装，可用ext4格式挂载
11.
(单选题)在~/.bashrc中添加alias qstat='/bin/ps -Ao pid,tt,user,fname,rsz' ，能使用qstat 命令的用户是（d）
 A.root用户
 B.student用户
 C.所有用户
 D.仅当前用户
12.
(单选题)配置DNS服务器时，将类型type设置为（b ）表示从区域记录。
 A.master
 B.slave
 C.hint
 D.forward
13.
(单选题)在RHEL7系统删除一个组的命令应该是什么( b)。
 A.A.groupmod
 B.B.groupdel
 C.C.userdel
 D.D.usermod
14.
(单选题)在rhel7上使用kickarstart生成ks文件yum仓库源ID应设置为（b ）。
 A.rhel7
 B.development
 C.默认名称
 D.ksckarstart
15.
(单选题)kickarstart 应答文件的包名是（ a）。
 A.system-config-kickstart
 B.kickstart
 C.system-config
 D.system-kickstart
16.
(单选题)当字符串用单引号（‘’）括起来时，shell将（c ）。
 A.解释引号内的特殊字符串
 B.执行引号中的命令
 C.不解释引号内的特殊字符
 D.结束进程
17.
(单选题)查询samba用户的命令（ c）。
 A.pdbedit -a 用户名
 B.id 用户名
 C.pdbedit -L 用户名
 D.pdbedit -x 用户名
18.
(单选题)如何彻底禁用selinux（a）。
 A.修改/etc/selinux/config文件中后重启服务器
 B.修改/etc/selinux/config文件中后重启服务
 C.使用setenforce 1命令禁用
 D.使用setenforce 0命令禁用
19.
(单选题)在安装linux操作系统时，必须分的两个分区是（b）。
 A.boot 和 data
 B.swap 和 根
 C.home 和 dev
 D.swap 和 boot
20.
(单选题)能够将KVM虚拟机直接连入物理网络、与其他主机相互通信的是（d ）连接模式
 A.root用户
 B.student用户
 C.所有用户
 D.仅当前用户
21.
(单选题)若要将名称为myrhel7的KVM虚拟机的配置文件导出，可以执行（a ）命令。
 A.A.virsh dumpxml myrhel7
 B.B. virsh list myrhel7
 C.C. virsh define myrhel6
 D.D. virsh destroy myrhel6
22.
(单选题)在RHEL7系统中使用rsync镜像备份时，为了保持源于目标的一致性，应该添加（ a）选项以清除目标位置的多余文档。
 A.--delete
 B.--checksum
 C.--clear-misc
 D.--reverse-sync
23.
(单选题)创建用户时使用选项（b）可指定用户id值。
 A.-d
 B.-u
 C.-s
 D.-G
24.
(单选题)发布安全NFS，部署的kerberos密钥文件放在那个文件夹下( a)。
 A.A./etc/
 B.B. /etc/kerberos
 C.C./kerberos/
 D.D./NFS/
25.
(单选题)为pxelinux.0启动程序提供配置文件是（ a）。
 A./var/lib/tftpboot/pxelinux.cfg/default
 B./var/lib/tftpboot/ks.cfg
 C./var/lib/tftpboot/initrd.img
 D./var/lib/tftpboot/vmlinuz
26.
(单选题) 扩展逻辑卷的命令为(d )。
 A.lvcreate
 B.vgcreate
 C.vgextend
 D.lvextend
27.
(单选题)以下那个命令可以列出/etc/文件夹的属性( d)。
 A.ls -lh /etc/
 B.ls -la /etc/
 C.ls -lAR /etc/
 D.ls -ld /etc/
28.
(单选题)下面环境变量（ a）表示获取当前执行的shell脚本的文件名。
 A.$0
 B.$1
 C.$@
 D.$*
29.
(单选题)为/dir 文件student用户的ACL权限，以下命令正确的是(a )。
 A.setfacl -m u:student:rwx /dir
 B.setfacl -s u:student:rwx /dir
 C.getfacl -s u:student:rwx /dir
 D.getfacl -m u:student:rwx /dir
30.
(单选题)在shell中变量的赋值有四种方法，其中采用name=tedu.cn的方法称（ a）。
 A.直接赋值
 B.使用read命令
 C.使用命令行参数
 D.使用命令的输出
31.
(单选题)使用grep过滤时选项（d）可以忽略大小写
 A.-v
 B.^
 C.$
 D.-i
32.
(单选题)格式化swap分区时使用什么命令( d)。
 A.mkfs.ext4 分区设备路径
 B.mkfs.xfs 分区设备路径
 C.swapon 分区路径
 D.swap分区不需要格式化
33.
(单选题)列出网上NFS共享资源的命令为（b ）。
 A.showmount 服务器地址
 B.showmount -e 服务器地址
 C.mount -e 服务器地址
 D.mount 服务器地址
34.
(单选题)若要判断整数变量A是否“小于或等于”B，可以使用（b）测试操作。
 A.[$A –lt $B]
 B.[$A –le $B]
 C.[$A –gt $B]
 D.[$A <= $B]
35.
(单选题)semanage port -a -t http_port_t -p tcp 8909 命令的含义是什么( c)。
 A.在semanage中删除http端口
 B.在semanage中添加http端口
 C.调整SELinux策略，允许Web服务使用8909端口
 D.调整SELinux策略，拒绝Web服务使用8909端口
36.
(多选题)在RHEL7中安装Mariadb数据库需要安装那些安装包( ab)。
 A.Mariadb-Server
 B.Mariadb
 C.MySQL
 D.MySQL-server
37.
(多选题)在RHEL7中配置LDAP需要安装那些软件包( bc)。
 A.sssh
 B.sssd
 C.authconfig-gtk
 D.authconfig-tui
38.
(多选题)快速构建WSGI动态网站是需要安装那些包（ad）
 A.httpd
 B.MYSQL
 C.PHP
 D.mod_wsgi
39.
(多选题)linux典型的文件系统类型有（bd）。
 A.NTFS
 B.ext4
 C.FAT32
 D.xfs
40.
(多选题)安装虚拟化服务平台需要安装那些包组( abc)。
 A."Virtualization Platform"
 B."Virtualization Host"
 C."Virtualization Client"
 D."Virtualization Tloos	"
41.
(多选题)常见的数据库软件有（abc）。
 A.DB2
 B.SQL Server
 C.MySQL
 D.virtualbox
42.
(多选题)如何永久配置用户主机名( ab)。
 A.修改/etc/hostname文件
 B.使用命令 hostnamectl set-hostname 主机名
 C.hostname 主机名
 D.host 主机名
43.
(多选题)使用cp命令时以下说法正确的是（cd）。
 A.cp -r 可以拷贝文件，但不能拷贝目录
 B.cp -f 可以拷贝目录
 C.cp -rf 可以拷贝文件和目录
 D.cp -p拷贝文件时可以保持文件原属性不变
44.
(多选题)selinux切换运行模式的方法（ac）。
 A.临时切换：setenforce 1或0
 B.临时切换：getenforce 1或0
 C.固定配置：/etc/selinux/config 文件
 D.固定配置：/etc/selinux/selinux.cnf
45.
(多选题)http虚拟主机的类型包括（bcd）
 A.A. 基于站点的虚拟主机
 B.B. 基于域名的虚拟主机
 C.C. 基于ip的虚拟主机
 D.D. 基于端口的虚拟主机
46.
(多选题)杀死一个进程的方式有( ab)。
 A.kill -9 PID
 B.killall -9 进程名
 C.Ctrl+z
 D.pkill -9 -u 用户名
47.
(多选题)把命令cp /ISO/1.txt /mnt放入后台的命令是(ab )。
 A.快捷键Ctrl+z
 B.cp /ISO/1.txt /mnt&
 C.cp /ISO/1.txt /mnt $
 D.Ctrl+c
48.
(多选题)安装源码包时需要安装编译工具有（ bcd）。
 A.C++
 B.gcc-c++
 C.gcc
 D.make
49.
(多选题)创建文件夹时指定目录的权限以下命令正确的是(ab )。
 A.mkdir -m 700 文件夹名称
 B.mkdir -m u=rwx,go=--- 文件夹名称
 C.mkdir -p 700 文件夹名称
 D.mkdir -P u=rwx,go=--- 文件夹名称
50.
(多选题)在/etc目录（含子目录）下查找文件名包含"passwd"的文件，可以执行（ bc）操作
 A.find /etc -name "passwd*"
 B.find /etc -name "*passwd*"
 C.ls -R /etc | grep 'passwd'
 D.ls -R /etc | grep '*passwd*'
交卷
关于我们 | 服务支持 | 咨询与反馈 | 最新动态 | 代理合作 | 名师堂
达内时代科技集团有限公司 2013-2014
中关村中心企业合作：62117598 UID中心企业合作：82168421、82168831
